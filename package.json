{
  "name": "linkbio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted link-in-bio page with JSON-file CMS and first-party click tracking.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "og": "node scripts/generate-og.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
